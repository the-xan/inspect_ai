# WEEK 2:

## Assignment 4.1: Plot CI width vs number of epochs

1. Посмотрите, как быстро полоса сужается.
   В какой момент запуск ещё одной эпохи перестаёт окупаться?
2. Увеличение K меняет вашу оценку accuracy модели или только вашу уверенность в ней?
3. Что это говорит о том, как распределять бюджет на оценку?

![Screenshot 2026-03-21 at 18.42.37.png](/Users/tim/PycharmProjects/inspect_ai/homework_week_2/mmlu/plot_ci_width_vs_number_of_epoch/Screenshot 2026-03-21 at 18.49.25.png)

**Ваш ответ:**

1. От K=5 до K=7 CI сужается заметно - это видно по тому, как зона уменьшается. После K=7-8 сужение замедляется и
   почти      
   останавливается. Значит, запуск 9-й и 10-й эпохи даёт очень мало прироста уверенности по сравнению со стоимостью
   запуска
2. Синия линия остаётся почти горизонтальной (~0.77) на всём диапазоне K. Незначительные колебания - это    
   шум (скорее всего), а не тренд. Значит, увеличение K не меняет оценку accuracy. Меняется только ширина CI, то есть
   наша
   уверенность в этой оценке.
3. Если у нас ограниченный бюджет на запуски, есть два решения:

- увеличить K (больше запусков на один и тот же набор вопросов)
- увеличить N (больше вопросов при K=5 (5-ти эпохах))

График показывает, что после K-7 отдача от роста K падает резко. Поэтому выгоднее держать 'K' небольшим (5-7) и
направить   
оставшийся бюджет на увеличение числа вопросов - это даст более репрезентативную оценку, чем многократный прогон одних
и  
тех же вопросов.
---

## Assignment 4.2: Compute and plot CI width vs n

1. При каком n линия accuracy начинает выглядеть стабильной?
2. Сравните это число с размером `MY_SUBSET` - находитесь ли вы в надёжной области?
3. Сравните эту кривую с кривой из 4.1. В чём разница между тем, что дают K и n?

**Ваш ответ:**

1. При каком n линия accuracy начинает
   стабилизироваться?                                                                  
   Примерно при n ≈ 60–80. До этого кривая резко падает и CI очень широкий (0.3–0.9). После 60–80 accuracy выходит на
   плато  
   0.35–0.40 и CI заметно сужается

2. Сравнение с размером MY_SUBSET - находимся ли мы в надёжной
   зоне?                                                      
   MY_SUBSET - 136 вопросов - это правый край графика, выше порога стабилизации (60–80). Мы находимся в надёжной зоне

3. Что даёт K, а что даёт n?
   Из сравнения двух графиков видна принципиальная разница:

- K (4.1) - уже при K=5 accuracy практически стабильна (0.77-0.78), рост до K=10 почти ничего не меняет. K
  лишь          
  незначительно уточняет вычисление CI на имеющихся данных. Отдача быстро насыщается.
- n (4.2) - при малых n результат скачет радикально, стабилизация медленная. n определяет статистическую         
  надёжность оценки

Итог: K и n решают разные задачи. Большой K при маленьком n всё равно даст широкий и ненадёжный CI - просто
точно         
посчитанный. Сначала нужен достаточный n, и только потом имеет смысл думать о K.

## Assignment 5: Compare two models

1. Запиши p-value и среднюю разницу, которые ты получил.
2. Является ли разрыв значимым? Достаточно ли он велик, чтобы иметь практическое значение?
3. Что могло бы изменить твой вывод: больше вопросов, другая тема или другая пара моделей?

**Ваш ответ:**

1. p-value = 0.0003, mean difference (A − B) = −0.32. Model_B превосходит model_A на 32 процентных пункта
2. 32 п.п. это очень большой разрыв. Для сравнения: в большинстве бенчмарков разрыв в 5–10% уже считается существенным.
   Здесь разница
   настолько велика, что выбор модели принципиален для любого реального применения
3. Больше вопросов - вывод о значимости стал бы ещё надёжнее, но при таком p-value он уже устойчив. Доверительный
   интервал на разницу сузился бы.

- Другая тема - вывод мог бы измениться. Разрыв между моделями зависит от области: model_A может быть сопоставима с
  model_B на темах, с которыми она лучше      
  знакома по обучению
- Другая пара моделей - наиболее вероятное изменение вывода. Разрыв в 32% характерен для сравнения слабой и сильной
  модели; у близких по классу моделей разница
  будет меньше и может оказаться незначимой при той же выборке

## Assignment 6: Estimate the accuracy gap

1. Запишите CI. Содержит ли он ноль?
2. Как это соотносится с тестом значимости из задания 5 — рассказывают ли они одну и ту же историю?
3. Какой результат более информативен — p-value или CI? Почему?

**Ваш ответ:**

1. CI: [−0.4827, −0.1573]. Нуль не содержит - вся область отрицательная, то есть llama2 хуже
   qwen2 при любом разумном сценарии.
2. Задание 5 дало p-value = 0.0003 -> разница значительна. Задание 6 дало
   интервал, не содержащий 0 -> тот же вывод. Это закономерно: если p < 0.05, то 95% CI для разницы не
   будет содержать 0, и наоборот. Два метода математически согласованы.
3. Интервал более информативен. p-value отвечает только на вопрос «случайна ли разница?» - бинарное
   да/нет. Интервал [−0.48, −0.16] говорит дополнительно: насколько велика разница и насколько мы в ней
   уверены. Например, даже нижняя граница −0.16 - это уже 16 процентных пунктов, что практически значимо.
   p-value = 0.0003 не даёт никакого представления о размере эффекта.

## Assignment 7: 'Estimate variance components'

1. Какой MDE получился для MY_SUBSET? Является ли этот порог практически значимым?
2. Если MDE больше наблюдаемой разницы из задания 5 — что это говорит о том результате?

**Ваш ответ:**

1. MDE = 16.7% при n=131 и одном прогоне каждой модели. Это очень высокий порог - чтобы эксперимент имел 80%         
   мощности, реальная разница между моделями должна быть больше 16 процентных пунктов. Для большинства практических
   задач такой порог не подходит: разница в 5–10% между моделями уже может быть важна при выборе, но здесь она была  
   бы статистически невидима.

2. В задании 5 разница составила ~32%, что больше MDE в 16.7%. Значит, эксперимент был достаточно мощным, чтобы      
   обнаружить именно эту разницу - и результат надёжен.
   Однако если бы реальная разница оказалась меньше 16.7%, то даже при её существовании мы не смогли бы отличить её  
   от шума с 80% вероятностью. Иными словами: отсутствие значимости в таком недостаточно мощном эксперименте нельзя
   интерпретировать как отсутствие разницы между моделями.

## Assignment 8: Implement `required_sample_size`

1. Сколько вопросов нужно, чтобы обнаружить разницу в 5%? В 10%?
2. Достаточно ли вопросов в `MY_SUBSET`, чтобы он был полезным бенчмарком для сравнения этих двух моделей?

**Ваш ответ:**

1. Для delta = 5% нужно **1466 вопросов**, для delta = 10% — **367 вопросов**.
   Числа большие, потому что llama2 нестабилен (sigma2_A = 0.20): на одном и том же вопросе
   при повторных прогонах он нередко даёт разные ответы. Этот шум увеличивает требуемую выборку.

2. MY_SUBSET содержит 131 вопрос - этого недостаточно даже для обнаружения разницы в 10%.
   MDE при n=131 составляет 16.7%, то есть любой реальный разрыв меньше этого порога
   статистически невидим.
   Тем не менее для сравнения именно llama2 и qwen2 выборки хватило: реальная разница (~32%)
   значительно превышает MDE, поэтому результат из задания 5 остаётся надёжным.
   Если бы мы сравнивали более близкие модели, 131 вопроса было бы мало.




# WEEK 1: Assignment 3: Analyzing Position Bias in Multiple Choice

## Анализ

> После запуска обоих evaluations открой `inspect view` и заполни результаты ниже.

---

### Результаты

| Условие                                    | Accuracy |
|:-------------------------------------------|:--------:|
| **Biased** - correct всегда на позиции `A` |  `0.0%`  |
| **Unbiased** - randomized positions        |  `0.3%`  |

#### Распределение выборов модели

| Позиция | Biased | Unbiased |
|:-------:|:------:|:--------:|
|    A    |   0    |    -     |
|  **B**  | **36** |  **36**  |
|    C    |   13   |    13    |
|    D    |   1    |    1     |

---

### Вопросы для ответа

**1. Что показывают числа?**
*Только факты - что ты наблюдал?*

> В biased evaluation (правильный ответ всегда на позиции `A`) модель ни разу не выбрала `A`.
> Вместо этого: `B` - 36 раз, `C` - 13 раз, `D` - 1 раз.
>
> В unbiased evaluation (случайные позиции) картина **идентична**:
> `B` - 36, `C` - 13, `D` - 1.

---

**2. Какие patterns ты замечаешь?**
*Например: какую позицию предпочитала модель? Была ли accuracy выше/ниже в biased condition?*

> Модель систематически предпочитает позицию `B`.
> Поскольку bias был именно в сторону `B`, при biased condition accuracy составила `0.0%`.
> При этом модель явно избегает ответ `A` из 120 вопросов именно этот вариант не был выбран ни разу.

---

**3. Что могло бы это объяснить?**
*Выдвини гипотезу. Какие факторы training или architecture могли это вызвать?*

> Скорее всего, при обучении модели правильный ответ чаще всего находился на позиции `B`.
> А на позиции `A` всегда был НЕ верный ответ.

---

**4. Что не вписывается в твоё объяснение?**
*Какие аномалии или counter-evidence ты заметил?*

> Складывается ощущение, что модель вообще не пыталась решить задачи -
> она выбирала позицию исходя из своей склонности, игнорируя содержание вопроса.

---

**5. Что ещё ты пробовал и что узнал?** *(опционально)*

> БОльшее количество вопросов делает эту склонность ещё более явной.
> Но при этом тренд был виден и на 10-ти запросах (Склонность к варианту `B` и игнорирование варианта `A`)
> Position bias - явно становится виден, когда модель не знает точного ответа.

---

## Bonus Challenges *(опционально)*

| # | Challenge            | Вопрос                                                        |
|---|----------------------|---------------------------------------------------------------|
| 1 | **Разные модели**    | Все ли модели показывают одинаковый bias pattern?             |
| 2 | **Chain-of-Thought** | Снижает ли `multiple_choice(cot=True)` position bias?         |
| 3 | **Больше позиций**   | Что происходит при 5 или 6 choices вместо 4?                  |
| 4 | **Statistical test** | Является ли предпочтение позиции значимым? (Chi-squared test) |
| 5 | **Content factors**  | Влияет ли сложность вопроса или тема на position bias?        |
